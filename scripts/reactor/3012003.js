function act() {
   //rm.dropSingleItem(4030021);
    //if (rm.hitBigby() == 0) {
    //    rm.resetMap(rm.getMap().getId());
    //} else if (rm.hitBigby() == 2) {
    //    rm.spawnMonster(9001051, 1, 185, 65);
    //}
}